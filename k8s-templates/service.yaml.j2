apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  namespace: {{ namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ app_name }}
  ports:
    - port: 8000
      targetPort: 8000
